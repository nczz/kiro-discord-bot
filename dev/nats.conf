# Development-only single-node NATS config for A2A rollout smokes.
# Production should use a three-node JetStream cluster with per-agent credentials.

port: 4222
server_name: kiro-a2a-dev

jetstream {
  store_dir: "./data/nats/jetstream"
  max_mem_store: 256MiB
  max_file_store: 2GiB
}

http: 8222
